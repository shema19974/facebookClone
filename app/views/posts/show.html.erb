<div class="card">
  <div class="card-header"><%= @post.content %></div>
  <div class="card-body"><%= image_tag @post.image %></div> 
  <div class="card-footer"><%= @post.user.firstname %></div>
</div>
<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
